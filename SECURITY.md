# 🔐 ОТЧЕТ ПО БЕЗОПАСНОСТИ - GRAFANA МОНИТОРИНГ

## 📋 Резюме

Проведен полный аудит безопасности конфигурации Grafana мониторинга для Docker контейнеров.
Обнаружено **7 критических уязвимостей** и **множество проблем средней степени опасности**.
Все уязвимости исправлены. Добавлен мониторинг 6 ML моделей.

---

## ❌ ОБНАРУЖЕННЫЕ УЯЗВИМОСТИ

### 🔴 КРИТИЧЕСКИЕ (High Severity)

#### 1. Хардкоженные учетные данные администратора
**Файл**: `docker-compose.yml:37-38`
**Проблема**:
```yaml
- GF_SECURITY_ADMIN_USER=admin
- GF_SECURITY_ADMIN_PASSWORD=admin
```
**Риск**: КРИТИЧЕСКИЙ
**CVSS**: 9.8 (Critical)
**Описание**: Пароль `admin/admin` - это первое, что проверяют злоумышленники. Полный доступ к Grafana.

**Исправление**: ✅
- Созданы `.env.example` и `.gitignore`
- Пароли вынесены в переменные окружения
- Добавлены дополнительные настройки безопасности Grafana

---

#### 2. Открытые порты без ограничений
**Файл**: `docker-compose.yml` (все сервисы)
**Проблема**:
```yaml
ports:
  - "9080:9080"   # Promtail - доступен всем
  - "3100:3100"   # Loki - доступен всем
  - "9090:9090"   # Prometheus - доступен всем
  - "3000:3000"   # Grafana - доступен всем
```
**Риск**: КРИТИЧЕСКИЙ
**CVSS**: 7.5 (High)
**Описание**: Все сервисы мониторинга доступны из интернета без аутентификации.

**Исправление**: ✅
- Все порты привязаны к `127.0.0.1` (localhost only)
- Конфигурируется через `.env` файл
- Создан безопасный Nginx reverse proxy с SSL и rate limiting

---

#### 3. Отключенная аутентификация в Loki
**Файл**: `loki-config.yaml:1`
**Проблема**:
```yaml
auth_enabled: false
```
**Риск**: КРИТИЧЕСКИЙ
**CVSS**: 8.2 (High)
**Описание**: Любой может читать все логи без авторизации. Логи могут содержать токены, пароли, персональные данные.

**Исправление**: ✅
- Добавлена поддержка аутентификации через переменную окружения
- Добавлены rate limits для защиты от DoS
- Добавлены ограничения размера запросов

---

#### 4. Privileged режим для Promtail
**Файл**: `docker-compose.yml:12`
**Проблема**:
```yaml
privileged: true
```
**Риск**: КРИТИЧЕСКИЙ
**CVSS**: 8.4 (High)
**Описание**: Контейнер имеет root доступ к хост-системе. При компрометации контейнера злоумышленник получает полный контроль над сервером.

**Исправление**: ✅
- Удален `privileged: true`
- Добавлен `security_opt: no-new-privileges:true`
- Docker socket монтируется в readonly режиме

---

#### 5. Незащищенный Docker socket
**Файл**: `docker-compose.yml:9`
**Проблема**:
```yaml
- /var/run/docker.sock:/var/run/docker.sock
```
**Риск**: КРИТИЧЕСКИЙ
**CVSS**: 9.0 (Critical)
**Описание**: Полный доступ к Docker API = root на хосте. Можно создать любой контейнер с привилегиями.

**Исправление**: ✅
- Socket монтируется в readonly режиме: `:ro`
- Добавлены security constraints

---

#### 6. Отсутствие мониторинга Docker контейнеров
**Проблема**: Нет инструментов для мониторинга ресурсов контейнеров
**Риск**: ВЫСОКИЙ
**Описание**: Невозможно отслеживать использование CPU/памяти/сети контейнерами.

**Исправление**: ✅
- Добавлен **cAdvisor** для метрик Docker контейнеров
- Добавлен **Node Exporter** для системных метрик хоста
- Настроен Prometheus для сбора метрик всех 6 ML моделей

---

#### 7. Небезопасный Dockerfile
**Файл**: `backend/Dockerfile`
**Проблема**:
- Запуск от root пользователя
- Нет проверок безопасности
- Большой размер образа

**Риск**: ВЫСОКИЙ
**CVSS**: 7.3 (High)
**Описание**: Приложение работает от root внутри контейнера.

**Исправление**: ✅
- Многоступенчатая сборка
- Создан непривилегированный пользователь `appuser`
- Добавлен healthcheck
- Минимизирован размер образа

---

### 🟡 СРЕДНЕЙ СЕРЬЕЗНОСТИ (Medium Severity)

#### 8. Отсутствие resource limits
**Проблема**: Нет ограничений CPU/памяти для контейнеров
**Исправление**: ✅
- Добавлены `deploy.resources.limits` для всех сервисов
- Конфигурируется через `.env`

#### 9. Отсутствие health checks
**Проблема**: Docker не знает о состоянии контейнеров
**Исправление**: ✅
- Добавлены healthcheck для всех сервисов

#### 10. Отсутствие restart policies
**Проблема**: Контейнеры не перезапускаются при сбое
**Исправление**: ✅
- Добавлен `restart: unless-stopped`

#### 11. Отсутствие сетевой изоляции
**Проблема**: Все контейнеры в одной сети
**Исправление**: ✅
- Создана отдельная сеть `monitoring`
- Создана сеть `app_network` для приложений

#### 12. Использование latest тегов
**Проблема**: Нет версионирования образов
**Исправление**: ✅
- Все образы используют конкретные версии

#### 13. Отсутствие логирования ротации
**Проблема**: Логи могут заполнить диск
**Исправление**: ✅
- Добавлены лимиты для логов Docker

---

## ✅ РЕАЛИЗОВАННЫЕ УЛУЧШЕНИЯ

### 🔐 Безопасность

1. **Управление секретами**
   - `.env` файл для всех паролей и токенов
   - `.gitignore` для предотвращения коммита секретов
   - Шаблон `.env.example` с инструкциями

2. **Сетевая безопасность**
   - Изолированные Docker сети
   - Порты привязаны к localhost
   - Nginx reverse proxy с SSL/TLS
   - Rate limiting для защиты от DDoS

3. **Контейнерная безопасность**
   - Непривилегированные пользователи
   - `no-new-privileges` для всех контейнеров
   - Read-only файловые системы где возможно
   - Отсутствие privileged режима

4. **HTTP Security Headers (Nginx)**
   - HSTS
   - X-Frame-Options
   - X-Content-Type-Options
   - X-XSS-Protection
   - CSP (Content Security Policy)
   - Permissions Policy

### 📊 Мониторинг

1. **Мониторинг контейнеров**
   - cAdvisor для метрик Docker контейнеров
   - Node Exporter для системных метрик

2. **Мониторинг 6 ML моделей**
   - service1 (порт 8501)
   - service2 (порт 8502)
   - service3 (порт 8503)
   - service4 (порт 8504)
   - service5 (порт 8505)
   - service6 (порт 8506)

3. **Метрики**
   - CPU, Memory, Network, Disk I/O
   - HTTP запросы и латентность
   - Логи из всех контейнеров

### 🏗️ Инфраструктура

1. **Resource Management**
   - CPU limits для всех сервисов
   - Memory limits для всех сервисов

2. **Health Checks**
   - Автоматическая проверка работоспособности
   - Автоматический перезапуск при сбоях

3. **Persistence**
   - Named volumes для данных
   - Retention policy 30 дней (Prometheus)
   - Retention policy 7 дней (Loki)

---

## 🚀 ИНСТРУКЦИЯ ПО РАЗВЕРТЫВАНИЮ

### 1. Подготовка

```bash
# Клонируйте репозиторий
git clone <your-repo>
cd grafana-deploy

# Создайте .env файл из шаблона
cp .env.example .env

# ОБЯЗАТЕЛЬНО: Измените пароли в .env файле!
nano .env
```

### 2. Настройка Nginx (опционально)

```bash
# Скопируйте конфиг
sudo cp nginx-secure.conf /etc/nginx/sites-available/grafana-monitoring

# Создайте симлинк
sudo ln -s /etc/nginx/sites-available/grafana-monitoring /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl reload nginx
```

### 3. Запуск мониторинга

```bash
# Запустите все сервисы
docker-compose up -d

# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs -f
```

### 4. Доступ к сервисам

- **Grafana**: https://dev.fielmann.co.ua/grafana/
- **Prometheus**: https://dev.fielmann.co.ua/prometheus/ (защищено)
- **ML Service 1-6**: https://dev.fielmann.co.ua/service1-6/

Логин Grafana (по умолчанию):
- Username: из `.env` файла (admin)
- Password: из `.env` файла (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)

---

## 🔍 МОНИТОРИНГ ML МОДЕЛЕЙ

### Метрики для каждой ML модели

Prometheus собирает следующие метрики:
- `up` - доступность сервиса
- `process_cpu_seconds_total` - использование CPU
- `process_resident_memory_bytes` - использование памяти
- `go_*` / `python_*` - метрики runtime

### Настройка для ваших ML моделей

Если ваши ML сервисы не предоставляют метрики Prometheus:

1. **Для Python (Flask/FastAPI)**:
```python
from prometheus_client import make_wsgi_app, Counter, Histogram
from werkzeug.middleware.dispatcher import DispatcherMiddleware

# Добавьте метрики
app.wsgi_app = DispatcherMiddleware(app.wsgi_app, {
    '/metrics': make_wsgi_app()
})
```

2. **Для других фреймворков**:
   - Используйте соответствующие библиотеки Prometheus client
   - Или следите только за метриками контейнера через cAdvisor

3. **Обновите prometheus.yml**:
```yaml
- job_name: 'ml-service1'
  static_configs:
    - targets: ['<container_name>:8501']  # Замените на имя контейнера
```

---

## 🛡️ ЧЕКЛИСТ БЕЗОПАСНОСТИ

### Перед запуском в production:

- [ ] Изменены ВСЕ пароли в `.env` файле
- [ ] `.env` файл добавлен в `.gitignore` и НЕ закоммичен
- [ ] Пароль Grafana минимум 16 символов
- [ ] SSL сертификаты настроены для домена
- [ ] Nginx конфигурация применена
- [ ] Rate limiting настроен в Nginx
- [ ] Firewall настроен (только порты 80, 443 открыты)
- [ ] Backup стратегия настроена для volumes
- [ ] Логи ротируются и не заполнят диск
- [ ] Мониторинг алертов настроен

### Регулярное обслуживание:

- [ ] Обновление Docker образов раз в месяц
- [ ] Проверка логов на подозрительную активность
- [ ] Ротация паролей каждые 90 дней
- [ ] Обновление SSL сертификатов (Let's Encrypt автообновление)
- [ ] Проверка использования дискового пространства
- [ ] Backup Grafana дашбордов и конфигураций

---

## 📊 АРХИТЕКТУРА МОНИТОРИНГА

```
┌─────────────────────────────────────────────────────────┐
│                      INTERNET                            │
└────────────────────┬────────────────────────────────────┘
                     │ HTTPS (443)
                     ▼
┌─────────────────────────────────────────────────────────┐
│                   NGINX (Reverse Proxy)                  │
│  - SSL/TLS Termination                                   │
│  - Rate Limiting                                         │
│  - Security Headers                                      │
└─────┬──────────────┬──────────────┬─────────────────────┘
      │              │              │
      │ /grafana     │ /service1-6  │ /prometheus
      ▼              ▼              ▼
┌─────────────┐  ┌──────────────┐  ┌──────────────┐
│   Grafana   │  │  ML Models   │  │  Prometheus  │
│   :3000     │  │  :8501-8506  │  │   :9090      │
└──────┬──────┘  └──────┬───────┘  └──────┬───────┘
       │                │                  │
       │ Query          │ Metrics          │ Scrape
       ▼                ▼                  ▼
┌─────────────┐  ┌──────────────┐  ┌──────────────┐
│    Loki     │  │   cAdvisor   │  │Node Exporter │
│   :3100     │  │    :8080     │  │   :9100      │
└──────▲──────┘  └──────────────┘  └──────────────┘
       │
       │ Push Logs
       │
┌──────────────┐
│   Promtail   │
│    :9080     │
└──────────────┘
       ▲
       │ Collect
       │
┌──────────────────────────────────────┐
│    Docker Containers Logs            │
│    /var/lib/docker/containers        │
└──────────────────────────────────────┘
```

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

### Документация
- [Grafana Security](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/)
- [Prometheus Security](https://prometheus.io/docs/operating/security/)
- [Docker Security Best Practices](https://docs.docker.com/engine/security/)
- [OWASP Docker Top 10](https://owasp.org/www-project-docker-top-10/)

### Инструменты для аудита
- [Docker Bench Security](https://github.com/docker/docker-bench-security)
- [Trivy - Container Scanner](https://github.com/aquasecurity/trivy)
- [Hadolint - Dockerfile Linter](https://github.com/hadolint/hadolint)

---

## 📝 ИСТОРИЯ ИЗМЕНЕНИЙ

### v2.0.0 - Безопасная конфигурация (Текущая версия)

#### Добавлено:
- ✅ cAdvisor для мониторинга Docker контейнеров
- ✅ Node Exporter для системных метрик
- ✅ Мониторинг 6 ML моделей через Prometheus
- ✅ Управление секретами через .env
- ✅ Nginx reverse proxy с SSL и security headers
- ✅ Network isolation (monitoring и app_network)
- ✅ Resource limits для всех контейнеров
- ✅ Health checks для всех сервисов
- ✅ Security constraints (no-new-privileges)
- ✅ Непривилегированные пользователи в контейнерах
- ✅ Многоступенчатая сборка Dockerfile
- ✅ Rate limiting в Loki
- ✅ Подробная документация по безопасности

#### Исправлено:
- 🔒 Хардкоженные пароли (admin/admin)
- 🔒 Открытые порты без ограничений
- 🔒 Отключенная аутентификация в Loki
- 🔒 Privileged режим для Promtail
- 🔒 Незащищенный Docker socket
- 🔒 Отсутствие мониторинга контейнеров
- 🔒 Небезопасный Dockerfile

#### Удалено:
- ❌ latest теги (заменены на конкретные версии)
- ❌ privileged: true
- ❌ Хардкоженные креды

### v1.0.0 - Исходная конфигурация
- Базовая настройка Grafana + Prometheus + Loki
- **НЕБЕЗОПАСНО - НЕ ИСПОЛЬЗУЙТЕ В PRODUCTION!**

---

## 👤 АВТОР

Аудит безопасности выполнен: AI Security Specialist
Дата: 2025-12-03
Статус: ✅ Все уязвимости исправлены

## 📧 КОНТАКТЫ

При обнаружении новых уязвимостей безопасности:
- Создайте issue в приватном репозитории
- Не публикуйте детали уязвимостей публично
- Следуйте responsible disclosure policy

---

## ⚠️ DISCLAIMER

Эта конфигурация значительно улучшает безопасность, но не гарантирует 100% защиту.
Безопасность - это процесс, а не конечное состояние.

**Рекомендации**:
- Регулярно обновляйте все компоненты
- Проводите периодические аудиты безопасности
- Мониторьте логи на подозрительную активность
- Используйте WAF (Web Application Firewall) если возможно
- Настройте автоматические бэкапы

---

**🔐 БЕЗОПАСНОСТЬ - ЭТО ПРИОРИТЕТ #1!**
