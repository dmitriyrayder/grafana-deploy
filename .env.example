# ===========================================
# БЕЗОПАСНАЯ КОНФИГУРАЦИЯ - GRAFANA МОНИТОРИНГ
# ===========================================
# ⚠️ ВАЖНО: Скопируйте этот файл в .env и измените все пароли!
# ⚠️ НЕ КОММИТЬТЕ .env файл в Git!

# --- GRAFANA ---
GF_SECURITY_ADMIN_USER=admin
# ⚠️ ОБЯЗАТЕЛЬНО СМЕНИТЕ ПАРОЛЬ! Используйте минимум 16 символов
GF_SECURITY_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE_MIN_16_CHARS

# Отключить регистрацию новых пользователей
GF_USERS_ALLOW_SIGN_UP=false

# Запретить анонимный доступ
GF_AUTH_ANONYMOUS_ENABLED=false

# --- LOKI ---
# Включить аутентификацию для Loki
LOKI_AUTH_ENABLED=true

# --- PROMETHEUS ---
# Базовая аутентификация для Prometheus (опционально)
PROMETHEUS_USER=prometheus_admin
PROMETHEUS_PASSWORD=CHANGE_ME_PROMETHEUS_PASSWORD

# --- NETWORK ---
# Ограничить доступ только к localhost (безопасно)
GRAFANA_HOST=127.0.0.1
PROMETHEUS_HOST=127.0.0.1
LOKI_HOST=127.0.0.1

# Или укажите IP вашего сервера для доступа через Nginx reverse proxy
# GRAFANA_HOST=0.0.0.0

# --- RESOURCE LIMITS ---
# Лимиты памяти для контейнеров
GRAFANA_MEMORY_LIMIT=512m
PROMETHEUS_MEMORY_LIMIT=2g
LOKI_MEMORY_LIMIT=1g
CADVISOR_MEMORY_LIMIT=256m

# --- ML SERVICES (ваши 6 моделей) ---
# Укажите имена контейнеров ваших ML сервисов
ML_SERVICE_1_NAME=service1
ML_SERVICE_2_NAME=service2
ML_SERVICE_3_NAME=service3
ML_SERVICE_4_NAME=service4
ML_SERVICE_5_NAME=service5
ML_SERVICE_6_NAME=service6

# Порты ваших ML сервисов (из nginx конфига)
ML_SERVICE_1_PORT=8501
ML_SERVICE_2_PORT=8502
ML_SERVICE_3_PORT=8503
ML_SERVICE_4_PORT=8504
ML_SERVICE_5_PORT=8505
ML_SERVICE_6_PORT=8506
